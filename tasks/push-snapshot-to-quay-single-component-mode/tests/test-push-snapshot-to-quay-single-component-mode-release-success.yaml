---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: test-push-snapshot-to-quay-single-component-mode-release-success
spec:
  description: |
    Run the push-snapshot-to-quay-single-component-mode task with a release param that refers to a successful release.
    The task should succeed.
  tasks:
    - name: run-task
      taskRef:
        name: push-snapshot-to-quay-single-component-mode
      params:
        - name: releasePlan
          value: "ns/releasePlan"
        - name: snapshot
          value: "ns/snapshot"
        - name: release
          value: "ns/success"
        - name: requireSuccessfulManagedRelease
          value: "true"
