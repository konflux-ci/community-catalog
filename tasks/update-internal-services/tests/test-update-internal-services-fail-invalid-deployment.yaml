---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: test-update-internal-services-fail-invalid-deployment
  annotations:
    test/assert-task-failure: "run-task"
spec:
  description: |
    Run the update-internal-services with an invalid `deployment` param and ensure the task fails.
  tasks:
    - name: run-task
      taskRef:
        name: update-internal-services
      params:
        - name: deployment
          value: other
        - name: repoUrl
          value: github.com/org/repo
        - name: githubSecret
          value: update-internal-services-secret
        - name: image
          value: quay.io/konflux-ci/internal-services:12345678
