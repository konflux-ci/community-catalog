---
apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: test-download-file-missing-secret
  annotations:
    test/assert-task-failure: "run-task"
spec:
  description: |
    Test handling of missing secrets for private repositories
  tasks:
    - name: run-task
      taskRef:
        name: download-file-from-git-repo
      params:
        - name: url
          value: "https://github.com/org/private-repo"
        - name: revision
          value: "main"
        - name: pathInRepo
          value: "config.yaml"
        - name: REPO_TOKEN
          value: "missing-secret"
        - name: REPO_KEY
          value: "token" 
